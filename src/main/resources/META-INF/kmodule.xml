<?xml version="1.0" encoding="UTF-8" ?>
<kmodule xmlns="http://www.drools.org/xsd/kmodule">
    <!--kbase 需要唯一的name,可取任意字符串,
        packages 为drl文件所在resource目录下的路径，需要根据实际填写，会默认扫描子目录， default 指定当前kbase是否为默认，一个
        moudle中顶多只允许有一个为default="true" ，里面ksession可以有多个-->
    <kbase name="addpoint" packages="hqh_addpoint" >
        <!--ksession name必须设置且要唯一， default:指定当前session是否默认-->
        <!--<ksession name="ksession_addpoint" default="false" />-->
        <!--<ksession name="ksession-subpoint" />-->
    </kbase>

</kmodule>
